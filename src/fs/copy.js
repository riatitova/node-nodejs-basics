// import * as fs from 'fs';
// import * as asyncFs from 'fs/promises';

// const folderExists = path => asyncFs.access(path, fs.constants.R_OK | fs.constants.W_OK).then(() => true, () => false);

export const copy = async () => {
    // Write your code here
    // const source = './files';
    // const destination = './files_copy';

    // let targetFile = destination;

    // If target is a directory, a new file with the same name will be created
    // if (await folderExists(destination)) {
    //     if (fs.lstatSync(destination).isDirectory()) {
    //         targetFile = path.join(target, path.basename(source));
    //     }
    // }

};

// copy().then();
